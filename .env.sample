###############################################
# Sample Environment File (.env.sample)
# Copy to .env (not committed) and fill values
# Do NOT place secrets in public variables.
###############################################

# Required server-only (never expose client)
PRIVATE_STOREFRONT_API_TOKEN=
SESSION_SECRET=
SHOP_ID=
PRIVATE_SHOPIFY_ADMIN_TOKEN=
BACKGROUND_ADMIN_KEY=

# Usage:
# - PRIVATE_SHOPIFY_ADMIN_TOKEN should have Admin API access with write_metaobjects.
# - BACKGROUND_ADMIN_KEY is a shared secret used to sign admin dashboard bearer tokens.

# Required public (safe for client bundle)
PUBLIC_STORE_DOMAIN=
PUBLIC_STOREFRONT_API_TOKEN=
PUBLIC_STOREFRONT_ID=
PUBLIC_STOREFRONT_API_VERSION=2024-10
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=
PUBLIC_CUSTOMER_ACCOUNT_API_URL=

# Optional
PUBLIC_CHECKOUT_DOMAIN=
PUBLIC_CANONICAL_HOST=

# Notes:
# - Keep API version in sync with Storefront client creation.
# - Do not commit a filled .env file.